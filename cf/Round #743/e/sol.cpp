// author: erray
#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  int n, k;
  cin >> n >> k;
  vector<int> X(n), Y(n);
  for (int i = 0; i < n; ++i) {
    cin >> X[i] >> Y[i];
  }

  vector<vector<int>> area(n, vector<int>(n));
  for (int i = 0; i < n; ++i) {
    for (int j = i + 1; j < n; ++j) {
      
    }
  }
}